#!/usr/bin/env python

import sys
import argparse

parser = argparse.ArgumentParser()
parser.add_argument('filename', help='alist txt file', default=None)
args = parser.parse_args()

if args.filename is None:
    fin = sys.stdin
else:
    fin = open(args.filename)

for line in fin:
    if line[0] == '5':
        if line[87] != line[88]:
            sys.stdout.write(line)
    elif line[0] == '6':
        if line[113] != line[114]:
            sys.stdout.write(line)
    else:
        sys.stdout.write(line)
